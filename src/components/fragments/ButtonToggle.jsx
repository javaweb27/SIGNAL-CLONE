import { container, circle } from "../../styles/fragments/button-toggle.module.scss"

const ButtonToggle = () => {
  return <div className={container}><span className={circle}></span></div>
}

export default ButtonToggle